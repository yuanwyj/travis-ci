language: node_js
node_js:
- '8'
cache:
  directories:
  - node_modules
install:
- npm install
before_script: 

addons:
    ssh_known_hosts: 39.108.125.0
script:
- npm run build
after_script:
# - ssh root@39.108.125.0 "./data/travis" # 请替换成自己的登录IP和登录用户
- scp -o stricthostkeychecking=no -r dist root@39.108.125.0:/data/travis
# - cd ./dist
# - git init
# - git config user.name "${U_NAME}"
# - git config user.email "${U_EMAIL}"
# - git add .
# - git commit -m "Update tools"
# - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:${P_BRANCH}
branches:
  only:
  - master
env:
  global: 
before_install:
- openssl aes-256-cbc -K $encrypted_5f4d1625ed6b_key -iv $encrypted_5f4d1625ed6b_iv
  -in id_rsa.enc -out ~/.ssh/id_rsa -d
- chmod 600 ~/.ssh/id_rsa
- echo -e "39.108.125.0ntStrictHostKeyChecking non" >> ~/.ssh/config
